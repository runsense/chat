import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 're.jcconception.rencontre',
  appName: 'rencontreIA',
  webDir: 'public'
};

export default config;
